---

---

<section>
  <div>
    <h2>Kom til en uforglemmelig dag</h2>
    <p>Tag med din virksomhed til en kreativ workshop hos FirePaperRun. Er I klar til at opleve enestående øjeblikke, som et afbræk fra det sædvanlige kontorliv? Den skabende proces vil binde jer sammen på en ny måde, der vil styrke jeres holdånd. </p>
    <p>I kan komme til mig, eller jeg kan komme til jer.</p>
      
  </div>
  <div>
    <a href="mailto:fire.paper.run@gmail.com">Send en efterspørgelse</a>
  </div>
</section>

<style>
  section {
    display: flex;
    flex-direction: column;
    gap: var(--padding-xl);
    background-image: url("/public/banner/om-os.webp");
    background-size: cover;
    background-position: center;
    padding: var(--default-inline);
    padding-block: 6rem;
    margin-block: var(--default-inline);
    margin-bottom: 5rem;

  }

  section div:first-child {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  h2,
  p,
  a {
    color: var(--light-white);
  }

  h2 {
    font-weight: var(--h2-weight-semibold);
  }

  a {
    text-decoration: underline;
  }

  a:hover {
    color: var(--light-orange);
  }

  @media screen and (min-width: 750px) {
    h2,
    p,
    a {
      color: var(--light-white);
    
    }
    section{
      margin-bottom: 7rem;
    }

    section div:first-child {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

  }
</style>
