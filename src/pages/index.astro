---
import MainLayout from "../layout/MainLayout.astro";
import FrontpageCard from "../components/FrontpageCard.astro";
import WorkshopCard from "../components/WorkshopCard.astro";
const data = await fetch("https://lcfiilplolafhgotzgrk.supabase.co/rest/v1/workshops", {
  method: "GET",
  headers: {
    apikey: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImxjZmlpbHBsb2xhZmhnb3R6Z3JrIiwicm9sZSI6ImFub24iLCJpYXQiOjE3MTQ3MzM4NjEsImV4cCI6MjAzMDMwOTg2MX0.NG1pwR1rYfymfHS_elIDmB5V-2TOWGrrPumjrgwhDp4",
  },
}).then((res) => res.json());

console.log(data);
---

<MainLayout>
  <section class="hero">
    <div><img class="baggrund" src="/banner/kopper-haand-1.webp" alt="" /></div>
    <div><img class="logo" src="/logo/fpr-hvid-stacked.svg" alt="" /> <p>Dette er en tekst</p></div>
  </section>
  <section class="frontpage_intro">
    <h2>Lær at arbejde med hænderne</h2>
    <p>Kom til workshop hos FirePaperRun og oplev enestående øjeblikke, som et afbræk fra det sædvanlige kontorliv.</p>
    <p>I Sydhavnen, i de smukkeste omgivelser, kan du lære at skabe unikaværker i keramik eller gips. Kun fantasien sætter grænser.</p>
    <div class="card_shelf">
      <FrontpageCard titel="At arbejde med gips" img="/produkter/gips.jpg" />
      <FrontpageCard titel="At arbejde med keramik" img="/produkter/keramik.jpg" />
    </div>
  </section>
  <section class="workshop_shelf">
    {
      data.map((workshop) => {
        return <WorkshopCard data={workshop} />;
      })
    }
  </section>
</MainLayout>
<style>
  .hero {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    grid-template-rows: 1fr 1fr 1fr;
    max-height: 450px;
    overflow: hidden;
    text-align: center;
  }
  .hero div:first-of-type {
    grid-area: 1 / 1 / 4 / 4;
  }
  .hero div:nth-of-type(2) {
    grid-area: 2 / 2 / 3 / 3;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
  }
  .logo {
    width: 12;
    max-width: 16rem;
  }
  .baggrund {
    object-fit: cover;
  }
  .frontpage_intro {
    margin-inline: var(--default-inline);
    padding-block: 1.8rem;
  }
  .workshop_shelf {
    margin-inline: var(--default-inline);
    padding-block: 1.8rem;
  }
  .frontpage_intro h2 {
    margin-bottom: 0.5rem;
  }
  .frontpage_intro p:nth-of-type(1) {
    margin-bottom: 1rem;
  }
  .card_shelf {
    display: flex;
    flex-direction: column;
    gap: 2.25rem;
    margin-block: 2rem;
  }
</style>
